import React from 'react';
import { StyleSheet, Text } from 'react-native';

const styles = StyleSheet.create({});

export const ProfileFavourites = () => {
    return (
        <Text style={{ fontSize: 48 }}>ProfileFavourites</Text>
    );
};