import React from 'react';
import { StyleSheet, Text } from 'react-native';

const styles = StyleSheet.create({});

export const ProfilePendingActivities = () => {
    return (
        <Text style={{ fontSize: 48 }}>ProfilePendingActivities</Text>
    );
};