import React from 'react';
import { StyleSheet, Text } from 'react-native';

const styles = StyleSheet.create({});

export const ProfileSkills = () => {
    return (
        <Text style={{ fontSize: 48 }}>ProfileSkills</Text>
    );
};